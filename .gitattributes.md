# Why `.gitattributes` says what it says

The rules are five lines. The reasoning behind them is long, was written twice
by two machines in the same round, and produced a merge conflict on four
consecutive exchanges — so it lives here instead. `.gitattributes` keeps the
rules and a pointer.

That is the first lesson and it is about this file, not about git: **two people
writing long prose into the same small config file will conflict on every
exchange, and the conflict is in the justification, never in the rule.** Both
sides independently arrived at the same three `merge=union` lines. Everything
that fought was commentary.

---

## `*.csv text eol=lf` and `*.csv2 text eol=lf`

`testapp/issues.csv` arrived with CRLF — the only tracked file in the repository
that had it — and the damage was not the line endings. Any tool that writes the
file correctly normalises them, so a one-field edit came back as a 183-line
diff, and such a diff is unreviewable: the one change that matters is hidden
among 182 that do not.

The `*.csv` rule was written to stop that, and then it happened anyway, because
**`*.csv` does not match `*.csv2`** and `.csv2` is the format this project
actually writes. `matrix_coverage/results.csv2` spent 2026-09-08 alternating
between CRLF and LF from one commit to the next — `68d46faa` LF, `bb7eac21`
CRLF, `543815c0` LF, `f9534bc6` CRLF — so a merge of a two-row append arrived
as a 641-line rewrite on both sides at once.

`git check-attr eol -- matrix_coverage/results.csv2` reported `unspecified`
while the same query on `testapp/issues.csv` reported `lf`. That is the whole
story: the guard had been written for the wrong glob, and five tracked `.csv2`
files were uncovered.

## `merge=union` on three append-only files

```
matrix_coverage/results.csv2 merge=union
mistakes/mistakes.csv2       merge=union
buildtime/buildtime.csv2     merge=union
```

Both machines write these and only ever append. Git has no append-aware merge
for CSV, so every merge where both had recorded a run came back as a conflict —
five in a row before the rule landed, each resolved by hand.

### Union is safe here because every record is exactly one physical line

Checked with a parser rather than assumed, for every tracked `.csv2`:

| file | records | lines |
| --- | --- | --- |
| `buildtime/buildtime.csv2` | 37 | 39 |
| `matrix_coverage/coverage-matrix.csv2` | 111 | 113 |
| `matrix_coverage/executable-size.csv2` | 49 | 51 |
| `matrix_coverage/results.csv2` | 706 | 708 |
| `mistakes/mistakes.csv2` | 112 | 114 |
| `testapp/P52-buttonstyle-findings.csv2` | 9 | 11 |

Two header rows each, so `records + 2 == lines` means no record contains an
embedded newline. Union merges **line by line**, so a quoted field holding a
newline would be split and interleaved: the rows either side of the join would
each become half a row, and nothing would report it.

**If a note ever needs a line break, the attribute has to go before that note
lands.** `mistakes.csv2` is the one to watch — its fields are prose.

```sh
recs=$(csv2 -r -i FILE | wc -l); lines=$(wc -l < FILE)   # safe when lines == recs + 2
```

### It must not be `merge=ours`, and it must not be a set

These files contain deliberate duplicate rows. The same app measured twice on
the same day is two observations, not one fact recorded twice. Measured
2026-09-09: `results.csv2` holds 706 data rows of which **98 are exact
duplicates of another row**. A deduplicating resolution would delete 98
measurements and report success.

That was the trap when the 2026-09-09 conflict was resolved by hand: a
set-based resolution dropped two rows, caught only because `685 + 3 + 2 = 690`
did not add up.

```sh
csv2 -r -i matrix_coverage/results.csv2 | sort | uniq -d | wc -l
```

### Known limit, and it fails loudly

Union merges the header rows too, so two sides editing a `.csv2` header in the
same round produce duplicated headers. `csv2` validates the header count and
refuses the file, so that surfaces as a hard error on the next read rather than
as corruption nobody notices. Both sides appending data never touches the
header.

---

## What is deliberately **not** covered, and why

| file | history | verdict |
| --- | --- | --- |
| `matrix_coverage/coverage-matrix.csv2` | 23 commits, +232 −119 | **matrix — rewritten in place** |
| `matrix_coverage/executable-size.csv2` | +100 −49 | **matrix — rewritten in place** |
| `testapp/P52-buttonstyle-findings.csv2` | **1 commit**, +11 −0 | one commit is not a pattern |

**Union does not conflict on an in-place edit — it keeps both versions of the
row.** Under union, P16's row in `coverage-matrix.csv2` would exist twice, once
saying pass and once saying fail, and a reader gets whichever they scan first.
That is worse than a conflict: a conflict stops and asks a human to look, while
union yields a matrix that looks complete, contradicts itself, and reports
nothing.

`buildtime.csv2` has only two commits, which is thin. It is listed, but "has
not been rewritten yet" is not "will not be", and it should be the first entry
removed if it ever grows an update path.

---

## Three wrong scopes, and what refuted each

Kept because each was refuted by a different kind of evidence, and the sequence
is more useful than the answer.

**1. `*.csv2` — refuted by asking what union actually guarantees.** The
one-line-per-record check above is real, and it was the wrong check. One line
per record makes union *non-destructive*; it does not make it *correct*. Union
is only correct for a file that is append-only.

**2. "`mistakes.csv2` is also unsafe" — refuted by counting diffs.** The
argument was that `csv2 -update 111:3` had been run against it. That update
rewrote a row the same commit had just appended, so the net was an append, and
67 commits show `+114 −0`. One remembered operation lost to `git log --numstat`.

**3. "`results.csv2` has 2001 deletions, so it is not append-only" — refuted by
looking at where the deletions are.** They are the CRLF/LF churn described
above, not row edits: the last twelve commits touching it are every one of them
`+N −0`. The lifetime total alone would have disqualified the file the rule
exists for.

### A measurement error worth not repeating

`git log --numstat -- <path>` filters which **commits** are shown and then
prints numstat for **every file** in each of them. Summing its columns yields
the whole repository's churn, not the file's. It reported **827,175 deletions
against a 114-line file** — roughly seven thousand full rewrites across
sixty-seven commits — and that was read as evidence before anyone noticed it
was arithmetically impossible.

Filter the numstat rows on the path as well:

```sh
git log --numstat --format= -- FILE | awk -v p=FILE '$3==p {a+=$1; d+=$2} END {print a, d}'
```

---

# `.gitattributes` 為何是現在這個樣子

規則只有五行。它們背後的理由很長，在同一輪中被兩台機器各寫了一次，並在連續四次交換上都造成合併衝突
——因此改放在這裡。`.gitattributes` 只留規則與一個指標。

那是第一個教訓，而且它關乎**這個檔案**、無關 git：**兩個人往同一個小型設定檔裡寫長篇散文，就會在每一次
交換上衝突，而衝突永遠發生在論證裡，不在規則上。** 雙方各自獨立地得出了完全相同的三行 `merge=union`。
真正在打架的全是註解。

## `*.csv` 與 `*.csv2` 的 `eol=lf`

`testapp/issues.csv` 帶著 CRLF 進來——它是整個 repository 中唯一如此的被追蹤檔案——而損害並不在行尾本身。
任何正確寫出該檔的工具都會把行尾正規化，於是一次「改一個欄位」的編輯回來時是 183 行的 diff，而那樣的 diff
無法審閱：真正重要的那一項改動，藏在 182 項不重要的改動之中。

`*.csv` 這條規則正是為了阻止那件事而寫的，然後那件事依然發生了，因為 **`*.csv` 匹配不到 `*.csv2`**，
而 `.csv2` 才是本專案實際會寫出的格式。`matrix_coverage/results.csv2` 在 2026-09-08 這天於各 commit 之間
反覆在 CRLF 與 LF 之間跳動，於是一次「追加兩列」的合併，在兩邊同時呈現為 641 行的整檔重寫。

`git check-attr eol` 對 `results.csv2` 回報 `unspecified`，對 `testapp/issues.csv` 回報 `lf`——這就是全部
的原因：守衛寫在了錯的 glob 上，而五個被追蹤的 `.csv2` 檔無一被涵蓋。

## 三個純附加檔案上的 `merge=union`

兩台機器都會寫這些檔案，而且只會附加。Git 對 CSV 沒有「理解附加」的合併方式，因此每一次「兩台機器都
記錄了執行結果」的合併都會變成衝突——在規則落地之前連續五次，每一次都是手工解的。

### union 之所以安全，是因為每一筆記錄恰好是一個實體行

以解析器查證、而非假設，且對**每一個**被追蹤的 `.csv2` 都查（表見上）。每個檔各有兩列標頭，因此
「記錄數 + 2 == 行數」即代表沒有任何記錄內含換行。union 是**逐行**合併的，因此一個「引號內含換行」的
欄位會被拆開並交錯：接縫兩側的列會各自變成半列，而且沒有任何東西會回報這件事。

**若日後某則備註需要換行，這條屬性必須在那則備註落地之前移除。** 要盯的是 `mistakes.csv2`——它的欄位
是散文。

### 它不可以是 `merge=ours`，也不可以是集合

這些檔案含有**刻意重複**的列。同一支 app 在同一天被量測兩次是兩筆觀測，不是同一個事實被記了兩遍。
2026-09-09 實測：`results.csv2` 有 706 個資料列，其中 **98 列與另一列完全相同**。任何會去重的解法都會
刪掉 98 筆量測並回報成功。

2026-09-09 那次手工解衝突時正是這個陷阱：以集合為基礎的解法掉了兩列，而是那組數字
（`685 + 3 + 2 = 690`）對不起來才抓到它。

### 已知限制，而且它會大聲失敗

union 連標頭列也會合併，因此若兩側在同一輪都改了某個 `.csv2` 的標頭，結果會有重複的標頭。`csv2` 會
驗證標頭數量並拒絕該檔，所以那會在下一次讀取時以硬性錯誤出現，而不是變成無人察覺的損壞。兩側都只是
附加資料時，根本不會碰到標頭。

## 刻意**不**涵蓋的檔案，以及理由

**union 對就地編輯不會衝突——它會把該列的兩個版本都留下。** 在 union 之下，`coverage-matrix.csv2` 中
P16 那一列會存在兩次，一份寫 pass、一份寫 fail，而讀者拿到哪一份取決於他先掃到哪一列。那**比衝突糟得多**：
衝突會停下來要求人去看，而 union 產出的是一個看起來完整、卻自相矛盾、且不會有任何東西回報的矩陣。

`buildtime.csv2` 只有兩次提交，證據很薄。它仍被列入，但「尚未被改寫」不等於「不會被改寫」，一旦它長出
任何更新路徑，它應該是第一個被移除的。

## 三個錯誤的範圍，以及各自是被什麼推翻的

保留下來，因為每一個都是被**不同種類**的證據推翻的，而那個順序比答案更有用。

**1. `*.csv2`——被「union 到底保證了什麼」推翻。** 上面那項「一筆一行」的查證是真的，而它是**錯的查證**。
一筆一行讓 union **不具破壞性**，卻不等於讓它**正確**。union 只對純附加的檔案正確。

**2.「`mistakes.csv2` 也不安全」——被數 diff 推翻。** 該主張的依據是有人對它執行過 `csv2 -update 111:3`。
但那次 update 改寫的是同一次提交剛剛附加的那一列，因此淨效果仍是附加，而 67 次提交顯示 `+114 −0`。
**用一次記得的操作去推導，輸給了 `git log --numstat`。**

**3.「`results.csv2` 有 2001 行刪除，所以它不是純附加」——被「去看那些刪除在哪裡」推翻。** 它們是上文
所述的 CRLF/LF 抖動，而非改列：最近十二次觸及它的提交，**每一次**都是 `+N −0`。若只看整段歷史的總數，
最需要這條規則的那個檔案反而會被判出局。

### 一個不該重複的量測錯誤

`git log --numstat -- <path>` 過濾的是**哪些 commit**，接著把那些 commit 中**每一個檔案**的 numstat 都
印出來。直接加總其欄位得到的是整個 repository 的變動量，不是該檔案的。它對一個 114 行的檔案回報了
**827,175 行刪除**——相當於六十七次提交裡有大約七千次整檔重寫——而在有人察覺那在算術上不可能之前，
它已經被當成證據讀過了。修法是連 numstat 的列也要以路徑過濾。
