action,x,y,origin,button,key,micros,note,platform
# P34 on Windows, GtkBackend: press "Show +100" and show that the visible row
# count actually moves.
#
# LAUNCH IT WITH -rows, or this file proves nothing. The whole file:
#   zsh testapp/run.zsh P34 --debug -rows 500 -actionfile actions/win/P34-show-more-rows.csv
# P34 defaults to rowCount 100 and starts with visibleRows already 100, so at
# the default BOTH buttons are no-ops: "Show +100" computes
# min(100, 100 + 100) = 100 and "Show all capped rows" assigns 100 over 100.
# Nothing on screen changes, and a file written that way would pass by doing
# nothing -- which is worse than no file. -rows 500 is what makes the button have
# work to do. This is a property of P34's defaults, not a bug this file is
# reporting.
#
# AND THE LOG DOES NOT SAVE YOU FROM THIS. Since 2026-09-04 the button logs
# unconditionally. MEASURED 2026-09-04 by replaying this exact file WITHOUT
# -rows, which is the whole reason the negative case was run rather than
# reasoned about:
#     P34 ... RENDER COMPLETE -- P34 rows=100
#     P34 ... show plus 100 -> visibleRows=100
# That is a real line, from a real click that really landed, carrying the exact
# value that ALSO means "the click missed both buttons" in the three-way reading
# below. Launched without -rows, a hit and a miss are indistinguishable in the
# log AND in the capture. The flag is not a convenience.
#
# THE EVIDENCE IS THE LOG AND THE CAPTURE TOGETHER. Since 2026-09-04 both
# buttons write, and they write DIFFERENT text -- "show plus 100 -> visibleRows="
# against "show all -> visibleRows=" -- so the log says WHICH one was hit, not
# merely that something was.
#
# What this header said until 2026-09-04, kept rather than deleted because it is
# what shaped every other choice below: "THE EVIDENCE HERE IS THE CAPTURE, NOT A
# LOG LINE, and that is weaker than most files in this directory ... Neither
# button writes anything. Do not read a matching log as confirmation that the
# click landed; it says only that the app started with the row count you asked
# for." That was true when written -- the only write calls were in .onAppear.
# The fix was two lines in P34.swift, not a rewrite of this file: the
# coordinate, the one-click design and the reasons for both are unchanged.
#
# WHY THIS BUTTON. Its neighbour is 8 points away and does something visibly
# DIFFERENT with the same control: "Show +100" takes visibleRows to 200,
# "Show all capped rows" takes it straight to 500. So a single number names which
# button was hit -- 200 for this one, 500 for the neighbour, and 100 for a click
# that missed both -- and that number now appears TWICE, in the header line on
# screen and in the log. Two independent readings of the same three-way test is
# the reason to drive the +100 button rather than the other one.
#
# ONE click. The row list under the buttons is inside a ScrollView pinned to
# .frame(height: 360), so adding 100 children does not move the buttons -- but
# the count that proves the click is the thing that changed, and re-reading it
# after a second click would mean re-measuring first. Keep the other button for
# its own file.
#
# Coordinates read off a wincap capture of the frame at 808x649 (P34 asks for
# 780x620 and GTK gives it more), GtkBackend, 2026-09-03, 100% display scale,
# origin=frame like every other file here. "Show +100" spans x 114..219,
# y 204..236; (166, 220) is its centre. "Show all capped rows" starts at x 227,
# which is why the centre and not an edge.
#
# BUILD IT WITH THE FLAG or this file does nothing at all:
#   SCUI_DEBUG=1 zsh testapp/compile.zsh -gtk4 P34
# GtkBackend guards its ActionFileReplay call with #if SCUI_DEBUG. Without it
# the replay is compiled out and the failure is silent -- run.zsh accepts the
# path, the app launches and renders, and not one line is printed. P34's own
# --debug flag is a SEPARATE gate, controlling whether the app writes its log at
# all -- including the click line this file now depends on. Without it the log
# file is never created and nothing says why.
#
# p34-debug-events.log lands in the CURRENT DIRECTORY, not testapp/output/ --
# P34 builds its path from currentDirectoryPath and run.zsh launches by absolute
# path without cd'ing. MEASURED 2026-09-04 after this file replayed with
# -rows 500, and this is the whole file:
#     P34 ... backend GtkBackend
#     P34 ... RENDER COMPLETE -- P34 rows=500
#     P34 ... show plus 100 -> visibleRows=200
# The capture carries the second, independent half. MEASURED: the header line
# reads
#     requested rows: 500; showing first 200
# That one line is the entire visible difference. The row list looks IDENTICAL
# before and after -- it is not scrolled, so both captures show Row 0 to Row 16
# and the 100 new children are all below the fold. Do not go looking for them in
# the picture and conclude the click failed.
#
# P34 於 Windows、GtkBackend：按下「Show +100」，證明可見列數確實有動。
#
# **必須以 -rows 啟動**，否則本檔什麼也證明不了。完整指令：
#   zsh testapp/run.zsh P34 --debug -rows 500 -actionfile actions/win/P34-show-more-rows.csv
# P34 的 rowCount 預設為 100，而 visibleRows 一開始就已經是 100，因此在預設值下**兩顆按鈕都是
# no-op**：「Show +100」算出 min(100, 100 + 100) = 100，「Show all capped rows」則把 100 指派
# 給 100。畫面沒有任何變化；照那樣寫出的檔案會「什麼都沒做就通過」——那比沒有檔案更糟。是
# -rows 500 讓這顆按鈕有事可做。這是 P34 預設值的性質，不是本檔要回報的 bug。
#
# **而 log 救不了這件事。** 自 2026-09-04 起該按鈕會無條件寫入。**2026-09-04 實測**：以本檔在
# **不帶 -rows** 的情況下重放一次——這正是把負向案例真的跑過、而不是用推理帶過的理由：
#     P34 ... RENDER COMPLETE -- P34 rows=100
#     P34 ... show plus 100 -> visibleRows=100
# 那是一行真實的、由一次真正命中的點擊所產生的訊息，而它帶的值恰好**也**是下方三向判讀中
# 「兩顆都沒按到」的那個值。若不帶 -rows 啟動，命中與落空在 log 與擷圖中**都**無從分辨。
# 這個旗標不是為了方便。
#
# **證據是 log 與擷圖兩者合起來。** 自 2026-09-04 起兩顆按鈕都會寫入，而且寫的是**不同**的訊息
# ——「show plus 100 -> visibleRows=」對「show all -> visibleRows=」——因此 log 能指出被按到的
# 是**哪一顆**，而不只是「有東西被按到」。
#
# 本標頭在 2026-09-04 之前的說法，保留而不刪除，因為它形塑了以下每一項選擇：「本檔的證據是擷圖，
# 而非 log 行，這比本目錄中大多數檔案都要弱……兩顆按鈕都不寫入任何東西。切勿把『log 內容相符』
# 當成點擊命中的佐證；它只說明 app 有以你要求的列數啟動。」那在當時是**正確的**——write 只在
# .onAppear 中被呼叫。修法是 P34.swift 裡的兩行，而非重寫本檔：座標、只點一次的設計，以及兩者
# 的理由都未改變。
#
# **為何選這顆。** 它的鄰居只相距 8 點，而且對同一個狀態做出**不同**的事：「Show +100」把
# visibleRows 帶到 200，「Show all capped rows」則直接帶到 500。因此單一個數字就能指出被按到的
# 是哪一顆——這顆是 200、鄰居是 500、兩顆都沒中則是 100——而那個數字現在會出現**兩次**：一次在
# 畫面的標頭行，一次在 log 裡。同一個三向測試有兩份互相獨立的讀數，正是選擇驅動 +100 而非另一顆
# 的理由。
#
# **只有一次點擊。** 按鈕下方的列表位於一個以 .frame(height: 360) 釘住的 ScrollView 內，因此多
# 加 100 個 child 並不會移動按鈕——但「證明點擊成立」的那個數字，正是被改動的東西；若要在第二次
# 點擊之後再讀它，就得先重新量測。另一顆按鈕留給它自己的檔案。
#
# 座標取自 2026-09-03 於 100% 顯示縮放下、808x649 的 wincap frame 擷圖（P34 要求 780x620，GTK
# 給了更大的），origin=frame。「Show +100」橫跨 x 114..219、y 204..236，(166, 220) 為其中心。
# 「Show all capped rows」自 x 227 開始，這正是取中心而非邊緣的原因。
#
# **必須帶旗標建置**，否則本檔完全不會有任何作用：
#   SCUI_DEBUG=1 zsh testapp/compile.zsh -gtk4 P34
# GtkBackend 以 #if SCUI_DEBUG 包住它呼叫 ActionFileReplay 之處。少了它，重放會被編譯掉，而
# 失敗是靜默的——run.zsh 接受路徑、app 正常啟動並繪製，卻一行都不印。P34 自身的 --debug 是
# **另一道**開關，控制 app 是否寫出 log——包括本檔現在所倚賴的那一行點擊訊息。少了它 log 檔根本
# 不會被建立，且沒有任何訊息說明原因。
#
# p34-debug-events.log 落在**目前的工作目錄**，而非 testapp/output/——P34 由
# currentDirectoryPath 組出路徑，而 run.zsh 以絕對路徑啟動且不做 cd。**2026-09-04 實測**，本檔
# 以 -rows 500 重放後，整個檔案就是這三行：
#     P34 ... backend GtkBackend
#     P34 ... RENDER COMPLETE -- P34 rows=500
#     P34 ... show plus 100 -> visibleRows=200
# 擷圖承擔第二份互相獨立的讀數。**實測**：標頭那一行顯示為
#     requested rows: 500; showing first 200
# 而那一行就是畫面上**全部**的差異。列表看起來前後**完全相同**——它沒有被捲動，因此兩張擷圖都只
# 顯示 Row 0 到 Row 16，新增的 100 個 child 全都在可視範圍之外。請勿在圖中找不到它們，就斷定
# 點擊失敗。
sleep,,,,,,900000,let the window settle and take focus,windows
click,166,220,frame,left,,,"centre of ""Show +100"" (x 114..219 y 204..236) -- header must read ""showing first 200""; 500 would mean the neighbour was hit and 100 that nothing was",windows
sleep,,,,,,1500000,hold for the capture and let the log catch up -- expect a third line reading show plus 100 -> visibleRows=200,windows
