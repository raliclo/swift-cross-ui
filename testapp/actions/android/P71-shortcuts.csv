action,x,y,origin,button,key,micros,note,platform
# P71 on Android: do menu shortcuts fire with no menu open, and does a DISABLED
# item stay silent?
#
# **IT PASSES NOW. It did not on 2026-09-23, it does on 2026-09-25, and the two
# days in between are the interesting part.**
#
#   2026-09-23   all 21 actions replayed, counters 0/0/0
#   2026-09-25   PLAIN 1, SHIFTED 1, DISABLED 0
#
# What was wrong was never the shortcuts: `adb shell input keycombination` fired
# all three from the first day. What was wrong was that the keys could not reach
# them, and two things had to be found before that was fixable.
#
#   1. Where the shortcuts lived. They were registered on the decor view as an
#      `OnUnhandledKeyEventListener`, which `ViewRootImpl` runs AFTER
#      `mView.dispatchKeyEvent` returns -- above anything an app can post to.
#      `AndroidSynthesiser` posts through `Activity.dispatchKeyEvent`, below it.
#      The way round that was tried and refused: `Instrumentation.sendKeySync`
#      needs INJECT_EVENTS, quoted in AndroidSynthesiser.swift.
#
#   2. That a key event follows the FOCUS CHAIN. `ViewGroup.dispatchKeyEvent`
#      dispatches to `mFocused` or to itself, and otherwise returns false -- it
#      does not walk its children. P71 focuses nothing, so the decor's `mFocused`
#      was null and no view below it saw the key at all. An instrumented build of
#      `ShortcutHostLayout` logged NOTHING across 21 replayed actions, which is
#      what turned this from a guess into a measurement.
#
# Both are now addressed by `ShortcutHostLayout`: it is the window's content
# view, it consults the same table after `super.dispatchKeyEvent` declines --
# the same moment "unhandled" meant, one level lower -- and it takes focus with
# FOCUS_AFTER_DESCENDANTS, so it holds it only when nothing else wants it.
#
# Checked for the two things that change could have broken:
#   nothing fires twice   after this file leaves PLAIN at 1, one system-injected
#                         Ctrl-S takes it to 2, not 3
#   focus still works     P70 on the same build reports `focused field: name`
#                         and `focus changes heard: 1`, the text field winning
#                         over the root exactly as FOCUS_AFTER_DESCENDANTS says
#
# **它現在會通過了。2026-09-23 不會,2026-09-25 會;而中間那兩天才是有意思的部分。**
#
#   2026-09-23   21 個動作全部重放完成,計數器 0/0/0
#   2026-09-25   PLAIN 1、SHIFTED 1、DISABLED 0
#
# 出問題的從來不是那些快捷鍵:`adb shell input keycombination` 從第一天就能觸發三個。出問題的是
# 「按鍵抵達不了它們」,而在那件事可修之前,得先找到兩樣東西。
#
#   1. 那些快捷鍵住在哪裡。它們註冊在 decor view 上、是一個 `OnUnhandledKeyEventListener`,
#      而 `ViewRootImpl` 是在 `mView.dispatchKeyEvent` 回傳**之後**才執行它——那在任何 app 投遞得到的
#      層級之上。`AndroidSynthesiser` 經由 `Activity.dispatchKeyEvent` 投遞,在它之下。
#      繞過去的辦法試過也被拒了:`Instrumentation.sendKeySync` 需要 INJECT_EVENTS,
#      那個例外引在 AndroidSynthesiser.swift 裡。
#
#   2. 一個按鍵事件跟著**焦點鏈**走。`ViewGroup.dispatchKeyEvent` 只把事件派給 `mFocused` 或自己,
#      否則直接回傳 false——它**不會**走訪子節點。P71 沒有聚焦任何東西,因此 decor 的 `mFocused` 是 null,
#      它底下沒有任何 view 看到過那個按鍵。一個對 `ShortcutHostLayout` 加了儀器的版本,在 21 個重放動作
#      之間**一行都沒印**——那正是把這件事從猜測變成量測的那一步。
#
# 兩者現在都由 `ShortcutHostLayout` 處理:它就是視窗的 content view,在 `super.dispatchKeyEvent`
# 拒絕之後查同一張表——與「unhandled」原本所指的是同一個時刻,只是低一層——而且它以
# FOCUS_AFTER_DESCENDANTS 取得焦點,因此只有在沒有別的東西要焦點時才持有它。
#
# 那個改動可能弄壞的兩件事,都查過了:
#   不會觸發兩次   本檔跑完讓 PLAIN 停在 1 之後,再送一次系統層級的 Ctrl-S,它變成 2、不是 3
#   焦點仍然正常   同一個版本上的 P70 回報 `focused field: name` 與 `focus changes heard: 1`,
#                 文字欄位贏過根節點,正如 FOCUS_AFTER_DESCENDANTS 所言
#
# **This file presses `control`, and the macOS one presses `command`, for the
# same three shortcuts.** That is not a discrepancy to be tidied away; it is the
# convention `EventModifiers.command` records, seen from the driving side.
# SwiftUI's `.command` is Command on Apple platforms and Ctrl everywhere else,
# so `.keyboardShortcut("s")` -- whose default modifier is `.command` -- is
# Cmd-S on macOS and Ctrl-S on Android. `AndroidBackend.keyModifiers(for:)` maps
# both `.command` and `.control` onto META_CTRL_ON, and
# `ApplicationShortcuts.matches` masks the incoming meta state to
# META_CTRL_ON | META_SHIFT_ON | META_ALT_ON before comparing.
#
# A `keydown command` row here would press KEYCODE_META_LEFT, whose META_META_ON
# bit that mask discards -- so the comparison would see no modifier at all and
# nothing would fire. The row would replay, the file would exit zero, and the
# counters would read 0/0/0, which is indistinguishable from a backend that
# never registered the shortcuts.
#
# **No menu is opened, which is the assertion.** On Android these go through
# `View.OnUnhandledKeyEventListener` -- it runs only after every view in the
# hierarchy has declined the key, which is what an app-level shortcut must do.
# A backend that had wired them to the menu's own `setAlphabeticShortcut` would
# need the menu open and would score 0 here. There is no menu bar on Android to
# open in the first place; `SwiftUnhandledKeyListener` says why.
#
# **The third row is the one worth having.** A backend that registers a key
# without consulting `isEnabled` fires the disabled item and the counter moves.
# The first two rows only prove keys arrive.
#
# No coordinates anywhere: a shortcut is not a place.
#
# P71 於 Android:在沒有任何選單開啟的情況下,選單快捷鍵會觸發嗎?而一個**已停用**的項目會保持沉默嗎?
#
# **這份檔案按的是 `control`,而 macOS 那份按的是 `command`,對應的卻是同樣那三個快捷鍵。**
# 那不是一個該被「整理掉」的不一致,而是 `EventModifiers.command` 所記載的慣例,從驅動端看過去的樣子。
# SwiftUI 的 `.command` 在 Apple 平台上是 Command、在其餘平台上是 Ctrl;因此
# `.keyboardShortcut("s")`(其預設修飾鍵就是 `.command`)在 macOS 上是 Cmd-S、在 Android 上是 Ctrl-S。
# `AndroidBackend.keyModifiers(for:)` 把 `.command` 與 `.control` 都對到 META_CTRL_ON,而
# `ApplicationShortcuts.matches` 在比較之前,會把收到的 meta state 遮罩成
# META_CTRL_ON | META_SHIFT_ON | META_ALT_ON。
#
# 此處若寫一列 `keydown command`,按下的會是 KEYCODE_META_LEFT,而它的 META_META_ON 位元會被那個遮罩
# 丟掉——於是比較端會看到「完全沒有修飾鍵」,什麼也不會觸發。那一列會照樣重放、檔案會以零結束、
# 而計數器會是 0/0/0——那與「一個從未註冊過快捷鍵的 backend」無法區分。
#
# **不開啟任何選單,而那正是斷言。** 在 Android 上這些走的是 `View.OnUnhandledKeyEventListener`
# ——它只在 view 階層中每一個 view 都拒絕了該按鍵之後才執行,而那正是一個 app 層級快捷鍵**必須**做的事。
# 一個把它們接到選單自己的 `setAlphabeticShortcut` 上的 backend,會需要選單開著,而在此處會得零分。
# Android 上根本沒有選單列可開;`SwiftUnhandledKeyListener` 寫了為什麼。
#
# **第三列才是值得擁有的那一列。** 一個「註冊按鍵卻不看 `isEnabled`」的 backend,會讓那個已停用的項目
# 觸發、計數器會動。前兩列只證明按鍵有抵達。
#
# 沒有任何座標:一個快捷鍵不是一個地方。
sleep,,,,,,2500000,let P71 lay out and the listener attach,android
keydown,,,,,control,,"hold Ctrl -- this is macOS's Command, see the header",android
sleep,,,,,,200000,,android
key,,,,,s,,"Ctrl-S -- PLAIN must fire, the readout going to 1 (VERIFIED 2026-09-25)",android
sleep,,,,,,300000,,android
keyup,,,,,control,,,android
sleep,,,,,,300000,,android
keydown,,,,,control,,,android
keydown,,,,,shift,,,android
sleep,,,,,,200000,,android
key,,,,,e,,"Ctrl-Shift-E -- SHIFTED must fire, and PLAIN must NOT: the comparison is exact, not contains (VERIFIED 2026-09-25: SHIFTED 1, PLAIN still 1)",android
sleep,,,,,,300000,,android
keyup,,,,,shift,,,android
keyup,,,,,control,,,android
sleep,,,,,,300000,,android
keydown,,,,,control,,,android
sleep,,,,,,200000,,android
key,,,,,d,,"Ctrl-D -- the DISABLED item; this one must NOT fire and its counter must stay 0 (VERIFIED 2026-09-25: no DISABLED line at all)",android
sleep,,,,,,300000,,android
keyup,,,,,control,,,android
sleep,,,,,,1200000,let the counters settle before the capture,android
